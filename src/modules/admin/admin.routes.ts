/**
 * @file src/modules/admin/admin.routes.ts
 * @description Platform Admin Management Routes
 * 
 * WHAT WILL BE DONE HERE:
 * - Endpoints: `GET /admin/mentors/applications`, `PATCH /admin/mentors/:id/approve`, `PATCH /admin/mentors/:id/reject`, `GET /admin/users`, `PATCH /admin/settings/commission`, `GET /admin/dashboard-stats`, `GET /admin/audit-logs`.
 */
