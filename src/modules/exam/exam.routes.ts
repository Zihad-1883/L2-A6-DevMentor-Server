/**
 * @file src/modules/exam/exam.routes.ts
 * @description Free Assessment Exam Routes
 * 
 * WHAT WILL BE DONE HERE:
 * - Endpoints: `POST /exams`, `GET /exams`, `GET /exams/:id`, `POST /exams/:id/attempt`, `POST /exams/attempts/:id/submit`, `GET /exams/attempts/my-history`.
 */
