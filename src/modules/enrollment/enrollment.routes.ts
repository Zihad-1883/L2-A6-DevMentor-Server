/**
 * @file src/modules/enrollment/enrollment.routes.ts
 * @description Program Enrollment Routes
 * 
 * WHAT WILL BE DONE HERE:
 * - Endpoints: `POST /programs/:id/enroll`, `PATCH /enrollments/:id/continue`, `PATCH /enrollments/:id/drop`, `GET /enrollments/my-enrollments`.
 */
