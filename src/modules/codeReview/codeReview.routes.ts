/**
 * @file src/modules/codeReview/codeReview.routes.ts
 * @description Code Review Marketplace Routes
 * 
 * WHAT WILL BE DONE HERE:
 * - Endpoints: `POST /code-reviews`, `GET /code-reviews/open`, `POST /code-reviews/:id/preview`, `POST /code-reviews/:id/claim`, `POST /code-reviews/:id/release`, `POST /code-reviews/:id/deliver`, `GET /code-reviews/my-requests`, `GET /code-reviews/my-claimed`.
 */
