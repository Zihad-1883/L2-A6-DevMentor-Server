/**
 * @file src/config/env.ts
 * @description Environment Variables Configuration & Validation
 * 
 * WHAT WILL BE DONE HERE:
 * - Define Zod schema to validate process.env variables at application startup.
 * - Enforce required variables (PORT, DATABASE_URL, DIRECT_URL, BETTER_AUTH_SECRET, etc.).
 * - Export a strongly-typed env configuration object for safe usage across the app.
 */
