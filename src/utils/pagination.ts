/**
 * @file src/utils/pagination.ts
 * @description Reusable Pagination & Sorting Helper
 * 
 * WHAT WILL BE DONE HERE:
 * - Parse page, limit, sortBy, and sortOrder query params into standard Prisma `skip`, `take`, and `orderBy` options.
 * - Format paginated response meta object `{ page, limit, total, totalPages }`.
 */
